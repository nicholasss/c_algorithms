#ifndef SELECTION_SORT
#define SELECTION_SORT

#include <stdlib.h>

// selection_sort sorts the array in place, it does not return a copy
void selection_sort(int *arr, int length);

#endif // SELECTION_SORT
